(jbuild_version 1)

(ocamllex (graphql_lexer))

(menhir (
  (modules (graphql_grammar))
))

(library (
  (name graphql_parser)
  (public_name graphql_parser)
  (libraries (result))
  (wrapped false)
))
